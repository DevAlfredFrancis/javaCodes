public class StudentRecord {

    public double computeFinalGrade(double quizGrade, double activityGrade, double examGrade) {
        double finalGrade ;
        return finalGrade = (quizGrade * 0.3) + (activityGrade * 0.3) + (examGrade * 0.4);
    }
}
